kind: Service
apiVersion: v1
metadata:
  name: todo-api
  labels:
    app: todo-api
spec:
  type: ClusterIP
  selector:
    app: todo-api
  ports:
    - name: tcp
      port: 8080
      protocol: TCP
      targetPort: 8080
---
kind: Deployment
apiVersion: apps/v1
metadata:
  name: todo-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todo-api
  template:
    metadata:
      labels:
        app: todo-api
        version: v1
    spec:
      containers:
      - name: todo-api
        image: todo-api
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
